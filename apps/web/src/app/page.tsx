import { ReportFlow } from '@/components/ReportFlow';

/** Landing page: the whole report journey lives here, progressively revealed. */
export default function HomePage() {
  return <ReportFlow />;
}
